import { Component } from '@angular/core';

/** Class that defines the structure of locationdetail. The structure is the same that is in the Location.cs */
export class LocationDetail {
  id: number;
  place: string;
}
